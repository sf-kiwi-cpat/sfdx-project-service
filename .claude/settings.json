{
  "permissions": {
    "allow": [
      "Bash(.claude/skills/cdd-common/scripts/find-my-prs *)"
    ],
    "deny": [
      "Bash(git * --no-verify*)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if [ -f package.json ] && [ ! -d node_modules ]; then echo \"Installing dependencies...\" >&2; npm install >&2; fi'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": ".claude/skills/cdd-common/scripts/check-needs-review"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const input=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); const file=input.tool_input?.file_path||''; if(file.includes('/spec/') && file.endsWith('.spec.ts')){console.error('BLOCKED: spec/ files are human-guarded contracts (SDLC 2026). Use /spec to create or modify them.');process.exit(1);}\""
          }
        ]
      }
    ]
  }
}
